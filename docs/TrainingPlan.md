# План тренинга для сотрудников

1. **Материалы:**
   - PDF-инструкция (экспортирована из `docs/EmployeeGuide.md`, файл `training/EmployeeGuide.pdf` в Google Drive).
   - Демо-видео (5 минут, показывает сценарий opener/closer) — ссылка: `https://drive.google.com/file/d/DEMO_RETAILCHECK_TRAINING`.

2. **Сценарий тренинга:**
   1. Вводное слово менеджера (5 мин).
   2. Просмотр демо-видео (5 мин).
   3. Практика с QR-кодами:
      - сотрудник А — «🟢 Открыть смену», проходит блок `open`;
      - сотрудник B — «↔ Продолжить смену», проверяет дневные сверки (dual mode);
      - сотрудник B — «🔴 Начать закрытие», завершает блок `close/finance`.
   4. Вопросы/ответы + чек-лист обратной связи.

3. **Чек-лист готовности:**
   - Сотрудник логинится в Telegram, подтверждаем, что он есть в whitelist.
   - Проходит сценарий `open` (сотрудник А) и `Продолжить/Закрытие` (сотрудник B) на тестовом магазине.
   - Загружает фото, пишет комментарий при дельте ≥ 300 ₽.
   - Подтверждает, что знает команды `/status`, `/summary`, `/export`.

4. **Фиксация результатов:**
   - Отметить каждого участника в Google Sheet `Training`.
   - Собрать вопросы и доработки → оформить тикеты.
