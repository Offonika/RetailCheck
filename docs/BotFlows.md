# Бот-флоу и сценарии

Документ описывает пользовательские сценарии Telegram-бота: состояние диалога, переходы и служебные ветки. Используется разработчиками при реализации FSM (finite state machine) и UX-сообщений.

## 1. Нотация
- **Состояния** — верхний регистр (`IDLE`, `STEP_INPUT`, `SUMMARY`, …).
- **Действия** — команды/кнопки (`/start`, «Далее», инлайн-кнопки).
- **Условия** — логические проверки (валидации, права).
- FSM реализуется на стороне бота (aiogram FSM + Redis). Для менеджеров и сотрудников используется одна FSM с разными ветками по роли.

## 2. Онбординг (`/start`)
1. Пользователь нажимает `/start` (без payload) или сканирует QR. Бот проверяет `tg_id` в листе `Users`.
2. Если пользователь не найден или не имеет доступа ни к одному магазину, отправляется локализованный текст `start.no_shops`/`start.no_access` из `locale/ru/messages.json`.
3. Для сотрудников с доступными магазинами бот отправляет приветствие `start.greeting` и строит пагинированный список магазинов (до 6 штук на экран). Каждая кнопка = название магазина → при нажатии открывается инлайн-клавиатура:
   - CTA «🟢 Открыть смену» (`start.button.open`) и «🔴 Начать закрытие» (`start.button.close`) запускают соответствующий поток и эквивалентны deep-link `?start=shop_<id>__open|__close`.
   - Для магазинов с флагом `dual_cash_mode = TRUE` после открытия смены появляется дополнительная кнопка «↔ Продолжить смену»: closer подключается к открытому run до финальной фазы и видит только свои шаги.
   - Кнопка «⬅️ К списку» возвращает пользователя к пагинированному списку магазинов, «Ещё…» отправляет подсказку использовать `/menu` для служебных действий.
4. Все сообщения, кнопки и ошибки берутся из `locale/ru/messages.json`; в коде нет жёстко заданных русских текстов. Менеджеры дополнительно используют `/manager` для своих действий, сотрудники — `/menu` для быстрых команд «Статус/Итог/Экспорт».
5. Если пользователь приходит по deep-link `?start=shop_<id>__open|__close`, бот минует список магазинов и сразу пытается назначить роль (см. раздел 3).
6. После выбора роли FSM переходит в `IDLE`/`STEP_INPUT` в зависимости от фазы; клавиши «Назад»/«Пропустить» описаны в соответствующих сценариях.

## 3. Сценарий сотрудника

### 3.1 Старт открытия (opener)
- **Триггер:** кнопка «Начать смену» или deep-link `?start=shop_<id>__open`.
- Бот проверяет, может ли пользователь работать с магазином (whitelist, `allow_anyone`).
- Захватывается Redis-lock `run:{shop_id}:{date}`:
  - Нет run → создаём `run(status=opened, opener=caller)`, записываем Audit `start_open`.
  - Run уже есть и `opener_user_id` пустой → назначаем текущего пользователя.
  - Run уже есть и `opener_user_id ≠ caller` → сообщение «Открытие уже у {username}» + подсказка обратиться к менеджеру.
- После успешного назначения бот переводит FSM в `STEP_INPUT`, подгружая дневные шаги opener: сначала `cash_start` и `photo_x_report`, затем шаги с `owner_role = both`, которые потом пройдёт и closer.
- Если `Shops.dual_cash_mode = TRUE`, в run фиксируем признак режима: шаги с `owner_role = both` будут записаны по обеим ролям; до подключения closer их закрывает только opener.

### 3.2 Старт закрытия (closer)
- **Триггер:** кнопка «↔ Продолжить смену» (дневные шаги B) или deep-link/кнопка «Начать закрытие» для финала.
- Бот убеждается, что для `(shop,date)` уже существует run. При «Продолжить смену» назначает closer и открывает такой же дневной список, как у opener, но без `cash_start` и `photo_x_report`.
- «Начать закрытие» доступно после дневных шагов: захватывается `run:{shop_id}:{date}:close`, closer подтверждает шаг `start_close` и загружает `photo_z_report`. Если closer не назначен, opener может закрыть смену.

### 3.3 Ввод шагов (`STEP_INPUT`)
Для каждого шага TemplateSteps (бот заранее фильтрует список по `owner_role`: opener видит только свои шаги, closer — свои, шаги `shared|both` доступны обеим ролям, но в RunSteps фиксируется фактический исполнитель):
1. Бот отправляет текст «Шаг X из N: <title>», подсказку, тип/обязательность и отображает клавиатуру (`/back`, `/skip` для необязательных шагов).
2. Пользователь вводит данные или загружает фото.
3. Валидации:
   - `number`: преобразование числа, проверка min/max, расчёт дельты относительно `norm`.
   - `text`: обязательность, пустые значения.
   - `check`: ответы «да/нет» в различных форматах.
   - `photo`: требуем наличие `telegram_file_id`.
   - `choice`: сравнение с допустимыми значениями (например, `terminal_choice` принимает только `T-Bank` / `Sberbank` / «Третий терминал»).
   - `photo_terminal`: до загрузки фото должен быть выбран терминал (через шаг `terminal_choice` или ввод); без выбранного `terminal_type` кнопка фото и «Далее» неактивны, пропуск недоступен.
4. При ошибке бот выводит причину, остаётся в `STEP_INPUT`.
5. После успешного ввода:
   - Рассчитываем `delta`.
   - Если |delta| ≥ threshold → переходим в под-состояние `WAIT_COMMENT`. Пользователь обязан отправить комментарий → возврат в `STEP_INPUT`.
   - Иначе записываем значение в `RunSteps`, показываем «Ок / дельта: …».
   - В `RunSteps` сохраняем `owner_role` и `performer_user_id`, чтобы менеджер видел, кто закрыл шаг (актуально для dual-mode). Для `terminal_choice` фиксируем выбор, для `photo_terminal` — и выбор, и фото (в Attachments привязаны к `owner_role`); шаг завершён только при наличии выбора и фото.
   - Каждое обновление шага (`status`, значения, комментарий) фиксируется в Audit (`action=step_update`).
6. Пользователь жмёт «Далее». FSM переходит к следующему шагу (сохраняем phase=open/close и `owner_role` в RunSteps).
7. Кнопка «Назад» доступна только для предыдущего шага и работает в режиме просмотра (значения показываются повторно, редактирование завершённых шагов заблокировано).
8. Кнопка «Пропустить» доступна только если `required = FALSE`.

### 3.4 Итог (`SUMMARY`)
1. После последнего обязательного шага дневного блока/закрытия бот предлагает `/summary <shop_id>`.
2. Перед формированием summary проверяем:
   - все обязательные шаги TemplateSteps (`required = TRUE`) закрыты у нужных ролей (включая `start_close` и `photo_z_report`);
   - нет шагов в `WAIT_COMMENT`;
   - для шагов с `|Δ| ≥ delta_threshold` выполнены условные требования: text/photo шаги с валидатором `delta_threshold` автоматически становятся обязательными (комментарий и т.п.).
3. Если проверки не пройдены → бот возвращает на первый проблемный шаг и пишет причину («Нужен комментарий по Δ», «Нет Z-фото»).
4. Команда `/summary shop_id`:
   - формирует карточку (opener/closer, кассы, терминалы, total Δ, статус Z-фото);
   - в режиме двух касс добавляет два блока: «Касса opener» и «Касса closer» с отдельными суммами/фото;
   - вызывает `RunService.finalize_run` (`status = closed`, `delta_rub`, `finished_at`);
   - пишет Audit `finish_close`;
   - отправляет менеджеру сообщение + inline «Вернуть/Передать роль/Экспорт».
5. FSM возвращается в `IDLE`.
6. Если менеджер использует «Вернуть на доработку»:
   - `Runs.status = returned`, очищаются `finished_at` и `delta_rub`, активный исполнитель сбрасывается.
   - Бот шлёт уведомление opener/closer в личку и в общий чат с причиной возврата и подсказкой заново пройти шаги.
   - Сотрудники снова нажимают `/start` → магазин → «Открыть/Продолжить/Закрыть», проходя чек-лист заново (старые значения перезаписываются).

## 4. Сценарий менеджера

### 4.1 Управление сменами
1. Основной сценарий — run создаётся автоматически при первом opener. Менеджер видит уведомление «Смена shop/date открыта пользователем X».
2. Команда `/create_run <shop_id> [YYYY-MM-DD]` остаётся как fallback: менеджер выбирает магазин/дату, бот создаёт run (`status = opened`) и отправляет приглашения.

### 4.2 Мониторинг, передача и возврат
1. Команда «Статус смен».
2. Без аргумента `/status` бот выводит список всех активных магазинов: статус, opener/closer, прогресс по шагам, суммарную дельту и ключевые незавершённые шаги.
3. `/status <shop_id>` показывает детальную карточку конкретного магазина.
3. Кнопки:
   - «Передать открытие» / «Передать закрытие» → менеджер выбирает сотрудника магазина → бот обновляет `opener_user_id` или `closer_user_id`, пишет Audit `handover_*` и уведомляет участников.
   - «Вернуть на доработку» → `Runs.status = returned`, проблемные `RunSteps.status = error`, сотрудникам прилетает список шагов для исправления.
4. Для ручного ввода доступна команда `/handover <shop_id> <open|close> <@username>`: мгновенно меняет закреплённого сотрудника с проверкой whitelist и записью `handover_*` в Audit.

### 4.3 Подтверждение итога
1. Когда сотрудник завершил смену, менеджер получает summary.
2. Кнопки:
   - «Ок» — подтверждаем, статус остаётся `closed`.
   - «Вернуть» — см. выше.
   - «Экспорт за день/неделю» — запускает Export Builder.
3. Команда `/export shop_id [YYYY-MM-DD]` создаёт запись в листе `Export` с агрегированными шагами и дельтой.

## 5. Напоминания
- Таймзона берётся из `Shops.timezone`; расчёт идёт от фактического времени назначения роли/старта закрытия.
- Поток A: напоминания через 15 мин, 30 мин, далее каждые 45 мин; после 18:00 по магазину — каждые 10 мин, пока обязательные дневные шаги A не закрыты.
- Поток B: напоминания через 15 мин, 25 мин, далее каждые 30 мин; после 20:00 — каждые 10 мин, пока обязательные дневные шаги B не закрыты. Если closer не назначен, поток не запускается.
- Поток закрытия: после шага `start_close` отправляем уведомления через 10 и 20 мин, далее каждые 30 мин, пока не загружено `photo_z_report`.
- Напоминания не прекращаются по времени и сбрасываются только после `status in (closed, returned)` или завершения обязательных шагов роли. Redis хранит timestamp последней отправки per run/роль для выдерживания интервалов.
- Джоб `delta_alerts` каждые `DELTA_ALERT_INTERVAL_MIN` минут суммирует дельту по RunSteps; если `|Δ| ≥ DELTA_THRESHOLD_RUB`, отправляет сообщение менеджеру и больше не повторяет до снижения дельты/закрытия смены.

## 6. Ошибки и эскалации
- **Проблема записи в Sheets:** бот сообщает «техническая задержка», кладёт событие в очередь повторов. Пользователь остаётся на текущем шаге, идентификатор шага повторно отсылается после успешной записи.
- **Сбой Google API (BrokenPipe/timeout):** `_execute_with_retry` делает до 3 попыток, ведёт счётчик ошибок и пишет alert в логи; после исчерпания попыток менеджер получает уведомление и ручной чек-лист переходит в режим ожидания.
- **Файл фото недоступен:** бот запрашивает повторную загрузку и не продвигает шаг.
- **Неизвестная команда:** бот выводит help с доступными командами по роли.

## 7. Тестовые сценарии (до разработки)
1. Сотрудник проходит все типы шагов (число, текст, чек, фото) → проверяем дельты, комментарии, итог.
2. Менеджер создаёт и возвращает смену → убеждаемся, что статус и шаги меняются корректно.
3. Напоминание перед открытием (за 15 мин) для незавершённой смены → сообщение приходит только один раз до закрытия.
4. Экспорт за день по кнопке → лист `Export` обновляется, Audit фиксирует событие.
5. Ошибка Google API → ретраи и уведомление, состояние диалога сохраняется.

Этот документ служит основой для реализации FSM и написания unit/e2e-тестов. Любые изменения в UX обязательно отражаем здесь перед код-ревью.
