# Инструкция для сотрудника RetailCheck

Документ объясняет, как открыть или закрыть смену в Telegram-боте чек-листа. Сохрани ссылку и пересылай новичкам.

## 1. Перед стартом
- Убедись, что тебя внесли в whitelist магазина (см. сообщение менеджера или список в Google Sheet).
- Проверь, что Telegram-бот RetailCheck добавлен в контакты.
- Под рукой должны быть цифры из кассы, терминалов и возможность сфотографировать Z-отчёт.

## 2. Как начать смену (opener)
1. Нажми `/start`, выбери свой магазин в списке и нажми кнопку «🟢 Открыть смену» **или** отсканируй QR `shop_<id>__open`.
2. Если никого ещё нет, бот автоматически закрепит тебя как opener и откроет первый шаг чек-листа.
3. Следуй подсказкам бота:
   - вводи числа только в рублях с точкой (бот сам округлит);
   - фото загружай прямо в чат (можно как изображение или «фото-документ»), по желанию добавляй подпись;
   - для каждого шага доступны кнопки «Назад» и «Пропустить» (если шаг не обязателен).
4. При отклонении ≥ 300 ₽ добавь комментарий — без него нельзя перейти дальше.

## 3. Как закрыть смену (closer)
1. Нажми `/start`, выбери магазин и нажми «🔴 Начать закрытие» **или** используй QR `shop_<id>__close`.
2. Если opener ещё не создал run, появится сообщение «Смена не создана» — попроси менеджера или opener начать.
3. Проходи шаги фазы `close`. Z-отчёт обязателен: загрузка фото — последний обязательный шаг.
4. После загрузки Z-фото нажми кнопку «Итог» и отправь `/summary shop_<id>`, чтобы бот собрал финальную сводку и отправил её менеджеру.

## 3.1 Как работать вдвоём (две кассы)
- Если магазин переведён в режим двух касс, второй сотрудник увидит кнопку «↔ Продолжить смену». Нажми её, чтобы пройти дневные сверки своей кассы, пока первый сотрудник остаётся opener-ом.
- Opener видит только свои шаги (касса A), closer — только свои (касса B). Общие шаги помечены ботом и доступны обоим.
- Напоминания после 11:00 приходят тем, кто назначен на соответствующую роль. Если видишь уведомление про «кассу B», оно предназначено closer-у.
- Итог `/summary` недоступен, пока обе роли не завершат обязательные шаги и не прикрепят фото (`fin_z_photo` остаётся обязанностью closer-а).

## 4. Полезные команды
- `/status shop_1` — показывает, кто уже открыл/закрыл смену, какие шаги заполнены и хватает ли Z-фото.
- `/summary shop_1` — отправляет итог менеджеру. Доступно только после завершения всех обязательных шагов, комментариев и фото.
- `/export shop_1 2025-01-10` — формирует запись в `Export` за указанную дату (без даты берётся текущий день) и присылает краткую справку.

`shop_1` в примерах — код магазина из QR/Google Sheet. Всегда подставляй свой `shop_id`, который показывает бот в списке магазинов и в `/status`.

## 5. Типовые ошибки и что делать
| Ошибка | Что значит | Как исправить |
| --- | --- | --- |
| «Доступ ограничен» | Тебя нет в странице `Users` или ты в неактивном магазине | Напиши менеджеру, чтобы добавили твой Telegram |
| «Открытие уже у …» | Другой сотрудник занял роль opener | Договорись с ним или попроси менеджера передать роль |
| «Закрытие уже у …» | Задача closer уже занята | Свяжись с closer-ом или попроси менеджера передать роль |
| «Смена не создана» | Closer начал раньше opener-а, run ещё нет | Попроси opener-а нажать «Начать смену» или сообщи менеджеру |
| Нельзя сформировать итог: нужен комментарий | В шагах с большой дельтой нет подписи | Открой указанный шаг, добавь текст, снова `/summary` |
| «Нельзя завершить смену без Z-фото» | Не загружено фото Z-отчёта | Вернись в шаг `z_report_photo`, добавь снимок |

## 6. Что делать после отправки summary
1. Дождись подтверждения менеджера или уведомления о возврате.
2. Если менеджер нажал «Вернуть», снова пройди проблемные шаги и повторно отправь `/summary`.
3. После закрытия можно переходить к следующей смене.
